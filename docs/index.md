# csharp

## Visão geral

csharp

## Ficha do componente

| | |
|---|---|
| Time responsável | dev |
| Tipo | service |
| Ciclo de vida | experimental |
| Tags | dotnet, csharp, api, net80 |

A ficha mostra os dados informados na criação do app. A fonte atual é sempre o
catálogo do Backstage: se o `catalog-info.yaml` mudar, atualize esta tabela.

## Onde ver este app

- **Catálogo do Backstage**: a página do componente `csharp` mostra o
  time, o ciclo de vida, as tags e as dependências.
- **Aba Kubernetes** da página do componente: pods, Deployment e Service em
  tempo real.
- **Página Pipelines** do Backstage: a última execução e o log de cada etapa.

Veja [Operação](operacao.md) para porta, acesso e saúde do app.
